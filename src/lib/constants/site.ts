export const SITE_URL = 'https://jakelundberg.dev';

// Default OG card used as the fallback for posts without a `cover` and as
// the sitewide og:image for every non-post page.
export const OG_DEFAULT_IMAGE = 'https://images.wraithcode.io/og/jakelundberg-og.png';
